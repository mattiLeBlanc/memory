# Memory Match game
How good is your memory? Find the matching cards as fast as you can.

# install
Run `npm intall` and then `npm run build`. Server the game from the `/dist` folder with a localhost server like `angular-http-server` or `http-server`.
